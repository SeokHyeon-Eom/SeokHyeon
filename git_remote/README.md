# git remote 실습
```
git remote 저장소를 보여줌
-v 옵션은 URL을 볼 수 있다.

git remote add [remote name] URL 새로운 remote생성

git remote rename [원래 이름] [변경 이름]

git remote remove [remote name]으로 삭제 할 수 있다.

push 할 때 git [romote name] [branch name]
```